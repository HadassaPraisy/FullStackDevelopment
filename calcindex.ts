import {Calculations} from './calc';

var ip=8;
var op=Calculations.square(ip);

console.log(`the square of ${ip} is ${op}`);


var ipf=5;
var opf=Calculations.factorial(ipf);

console.log(`Factorial of ${ipf} is ${opf}`);